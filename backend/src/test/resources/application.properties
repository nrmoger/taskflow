# Test-specific properties to use an in-memory H2 database so ApplicationContext
# can start without requiring a local PostgreSQL instance.
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Use Hibernate's H2 dialect for tests and create/drop schema between runs
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Disable H2 console in tests
spring.h2.console.enabled=false

